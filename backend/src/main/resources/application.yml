spring:
  application:
    name: devconnect-backend
  datasource:
    url: jdbc:mysql://localhost:3307/devconnect_db
    username: root
    password: devconnect123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

jwt:
  secret: devconnect-super-secret-key-must-be-at-least-32-chars
  expiration: 86400000